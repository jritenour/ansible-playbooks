---
- hosts: localhost
  remote_user: root
  tasks:
    - name: convert VM
      shell: 'ovftool --noSSLVerify vi://root:'{{ password }}'@192.168.2.5/rhel6test ~/migration.ova'
    - name: upload test
      s3:
        aws_access_key: '{{ aws_access_key }}'
        aws_secret_key: '{{ aws_secret_key }}'
        bucket: jr-imageupload
        object: images/testimage.ova
        src: /root/migration.ova
        mode: put
